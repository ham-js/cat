import { Devices } from "../../../src/devices/Devices.ts"
import { DeviceTypes } from "./DeviceTypes.ts"
import { DeviceVendors } from "./DeviceVendors.ts"

# Overview

:::tip

This is a live document. Look at the source code ("Edit this page" at
*the bottom) in order to understand how we receive this information.

:::

The following devices are supported:

<table>
  <thead>
    <tr>
      <td>Device Type</td>
      <td>Device Vendor</td>
      <td>Device Name</td>
    </tr>
  </thead>
  <tbody>
    {
      Devices.map(({deviceName, deviceType, deviceVendor}) => (
        <tr>
          <td>{DeviceTypes[deviceType]}</td>
          <td>{DeviceVendors[deviceVendor]}</td>
          <td>{deviceName}</td>
        </tr>
      ))
    }
  </tbody>
</table>
